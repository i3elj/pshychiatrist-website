import './HeroSection.css'

const HeroSection = () => (
	<section id="hero">
		<h1>Welcome to Our Clinic</h1>
		<p>There is no better goal for us than to help you achieve your maximum.</p>
		<button class="btn-primary">Make an appointment</button>
	</section>
)

export default HeroSection
